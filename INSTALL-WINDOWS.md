# Windows 一键安装

[返回首页](README.md) · [Linux 安装](INSTALL-LINUX.md) · [手动安装](README.md#安装) · [反馈问题](https://github.com/a86582751/dsh-nexttavern/issues/new/choose)

`NextTavern-Setup.exe` 是给玩家的推荐安装方式：不用自己装运行时、不用自己配补丁，双击、选目录、等待，就能在自己的电脑上打开角色扮演工作台。

## 它替你做了什么，需要你做什么

| 安装器负责 | 需要你 |
| --- | --- |
| 准备便携的 PowerShell 7.6.6 和 Node.js 24.16.0 运行时，不占用你系统里已有的 Node。 | 确认安装目录（或直接用默认目录）。 |
| 按镜像优先的顺序下载主包与依赖，逐文件校验 SHA-256，并缓存已校验的文件。 | 有可用的网络；需要代理时填写代理地址。 |
| 自动检查并按需安装 Microsoft Visual C++ 运行库。 | 如果系统提示需要管理员授权，允许一次即可；已装过则完全跳过。 |
| 建立工作区、桌面快捷方式和开始菜单的启动/停止入口。 | 第一次打开后，在浏览器里填写自己的模型供应商和 Key。 |
| 把本机服务绑定在 127.0.0.1，并在 3510–3599 之间自动挑一个空闲端口。 | 不需要改动路由器、防火墙或系统设置。 |

**它不会替你准备模型。** API Key 始终由你自己填写、只保存在本机，安装包不携带、也不分发任何密钥。

## 三步完成安装

1. **运行安装器。** 双击下载到的 `NextTavern-Setup.exe`（64 位系统用它），Windows on ARM 设备使用 `NextTavern-Setup-arm64.exe`。它会打开一个原生窗口，不需要额外的框架或运行库。
2. **选择安装文件夹。** 默认是 `%LOCALAPPDATA%\NextTavern`，也就是你自己账号的本地应用目录。建议保持默认；如果想改，请选择本机磁盘上的普通目录，不要用网络共享路径。
3. **等待，然后开始。** 窗口里会显示当前阶段和下载进度。完成后浏览器会自动打开工作台；第一次进入时按提示选择工作区、填写模型供应商，然后在新建会话的预设菜单里切到“角色扮演模式”。

安装过程中途关掉窗口不会留下半套可用环境：重新运行安装器会继续复用已经下载并校验过的文件，不必从头再来。

## 安装到哪儿，怎么再打开

安装根目录下会建立这些子目录：

| 目录 | 用途 |
| --- | --- |
| `runtime` | 便携的 PowerShell 与 Node.js 运行时。 |
| `harness`、`home` | 本地服务本体，以及你的 DSH 配置目录。 |
| `workspace` | 角色扮演工作区：角色卡、故事资源和导出文件。 |
| `downloads` | 已下载并校验过的安装文件，重试和升级时复用。 |
| `logs` | 安装与服务日志。 |
| `backups`、`temp` | 升级备份与临时文件。 |

再打开有两种方式：

- **桌面上的 `NextTavern` 快捷方式**：启动服务并打开浏览器。
- **开始菜单**：`NextTavern` 为启动，`Stop Next Tavern` 为停止。

服务在跑的时候重复点“启动”只会复用已经运行中的实例，不会叠加第二个。

## 下载来源、代理与缓存

- **来源**：默认使用镜像源，速度通常更稳定；如果镜像不可用或你想直连，可以在界面里切换到 GitHub 源。
- **代理**：界面上有代理输入框。填了就按它走；留空则使用系统默认网络设置。
- **缓存**：每个文件下载完成后立即校验 SHA-256，合格的文件留在 `downloads` 里。中断、失败或重试时，已经校验通过的文件会被直接复用，不需要重新下载。

## 网络中断了怎么办

直接重新运行安装器即可。它会跳过已经校验合格的运行时和已下载文件，只补下缺失的部分，然后继续安装。如果失败信息提示某个来源不可用，切换另一个下载来源或填写代理后重试，通常就能继续。安装失败时诊断记录会写在 `logs\bootstrap.log` 和 `logs\install.log`，提交反馈时可以附上它们（先确认其中没有你的密钥或私密内容）。

## 升级已有安装

**0.2.5 这一版新增了原地升级。** 在旧安装上直接运行新版本的安装器，它会识别已有安装、保留你的故事、角色卡、工作区和模型设置，只替换需要更新的部分；升级前会在 `backups` 下留下备份，方便回滚。

两点前提：

- 如果服务正在运行，先用 `Stop Next Tavern` 停下来再升级。
- 升级是有版本范围的。安装器只接受它登记过的旧版本；版本不匹配或指纹不符时会明确停止并说明原因，不会降级成一套半新半旧的环境。

## 安全边界

- **只监听本机。** 服务绑定 `127.0.0.1`，端口在 3510–3599 之间自动选择，不对局域网或公网开放。
- **不动系统。** 安装器不修改 PATH、不写系统级注册表项、不改动全局 npm 注册表；它使用便携运行时，与你机器上现有的 Node.js 互不影响。
- **密钥自己填。** 模型 API Key 由玩家输入并保存在本机配置中，安装包不包含任何密钥。
- **想开放公网需要额外一步。** 手机／外网访问请按[手机电脑公网访问指南](PUBLIC-ACCESS.md)显式配置，默认安装不会自动开放网络。

## 端口、日志与进阶用法

**端口。** 安装器不提供固定端口选项：它在 3510–3599 中自动挑一个空闲端口，并写进安装根目录的 `running.json`（当前运行端口）和 `installation.json`（安装记录）。想确认当前端口，看这两个文件，或者直接看你浏览器里打开的地址。如果你需要固定端口来做端口转发或反向代理，请改走[手动安装](README.md#安装)路径，在那里可以显式指定 `--port`。

**日志。** 位置都在安装根目录的 `logs` 下：

| 文件 | 内容 |
| --- | --- |
| `install.log` | 安装各阶段的记录。 |
| `bootstrap.log` | 安装器引导阶段的错误记录。 |
| `server.stdout.log` | 本地服务的标准输出，启动地址也在这里。 |
| `server.stderr.log` | 本地服务的错误输出。 |

**给进阶用户。** 安装器同时支持命令行参数（`--root`、`--source Mirror|GitHub`、`--proxy`、`--no-open`、`--no-shortcut`、`--yes` 无界面模式）。普通玩家不需要用到它们；如果你希望把安装接进自己的脚本，可以在安装目录里运行 `pwsh -File NextTavern-Setup.ps1 -Action Start` 来手动启停。

## 卸载与回滚

**回滚一次升级。** 停止服务，然后用 `backups` 里对应的备份目录还原安装根目录，再启动即可。

**完全卸载。** 停止服务后，删除桌面和开始菜单里的 `NextTavern` / `Stop Next Tavern` 快捷方式，再删除整个安装根目录（默认 `%LOCALAPPDATA%\NextTavern`）。

**你的故事在哪。** 角色卡、故事文本、笔记、导出文件和模型配置都在安装根目录的 `workspace` 与 `home` 里。删除根目录会一并删除它们，所以想保留内容时，先单独备份这两个目录。

## 我们还不能保证的事

- **安装器没有代码签名。** 首次运行时 Windows SmartScreen 可能弹出提示，这是未签名程序的正常表现，不代表安装包被篡改。
- **没有在全新 Win11 虚拟机和真实 ARM64 设备上验证过。** x64 的 `NextTavern-Setup.exe` 是主要测试路径；`NextTavern-Setup-arm64.exe` 已构建并可供 ARM 设备使用，但作者的验证范围有限，欢迎反馈实际结果。
- **安装需要网络。** 便携运行时和主包在安装时下载，首次安装不可避免需要联网。
- **Visual C++ 运行库若需新装，会请求一次管理员授权。** 已安装且版本足够时会自动跳过。
- 安装目录请使用本机磁盘路径（不支持 `\\` 网络路径）。

## 下载

- [NextTavern-Setup.exe（x64）](https://github.com/a86582751/dsh-nexttavern/releases/download/v0.2.5/NextTavern-Setup.exe)
- [NextTavern-Setup-arm64.exe（Windows on ARM）](https://github.com/a86582751/dsh-nexttavern/releases/download/v0.2.5/NextTavern-Setup-arm64.exe)
- [SHA256SUMS](https://github.com/a86582751/dsh-nexttavern/releases/download/v0.2.5/SHA256SUMS) 与[版本说明](https://github.com/a86582751/dsh-nexttavern/releases/tag/v0.2.5)

想自己控制运行时、自己应用兼容补丁，或者需要固定端口，请走首页的[手动安装](README.md#安装)；两条路径产出的都是同一套 preset 与同一套补丁。
